## Overview

This repository demonstrates API testing activities performed for the Healthcare Case Management System (HCMS).

The repository showcases common API validation activities including CRUD operations, import/export validation, positive and negative testing, and execution reporting.

---

## Objective

The objective is to validate backend services independently from the user interface to ensure data integrity, business logic, and API reliability.
